# --- API & Web ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# --- Worker / Queue ---
celery[redis]>=5.4.0

# --- LangChain / RAG ---
langchain==0.1.14
langchain-community==0.2.19
chromadb>=0.4.24
sentence-transformers>=2.2.2
pyyaml>=6.0.1

# --- その他（後々必要になる可能性が高い） ---
httpx>=0.27.0          # LLM呼び出しやAPIテストに
pydantic-settings>=2.1.0  # `.env` 読み込み用
sqlmodel>=0.0.16       # DBが必要になる場合
python-multipart>=0.0.6

# --- OpenAI ---
openai>=1.24.0
langchain-openai>=0.1.6
